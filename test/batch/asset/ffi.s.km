(module
  ffi
  (list)
  (list
    (define unit (list unit))
    (bind-value-foreign
      hello
      (list (system-header "stdio.h"))
      """
        void ffi_hello(void) {
          printf("hello\n");
        }
      """
      (procedure unit))))
