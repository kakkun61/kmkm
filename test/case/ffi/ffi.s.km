(module
  ffi
  (list
    kmkm.unit)
  (list
    (bind-value-foreign
      hello
      (list (system-header "stdio.h"))
      """
        void ffi_hello(void) {
          printf("hello\n");
        }
      """
      (procedure kmkm.unit.unit))))
