(module
  parametricPolymorphism
  (list
    kmkm.prim)
  (list
    (bind-value id (for-all a (function a a a)))
    (bind-value hello (apply id "Hello world!"))
    (define solo (for-all a (list (solo a))))))
